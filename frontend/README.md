# SecureVision Frontend (React + Vite, NO Tailwind)

## Run
```bash
cd frontend
npm install
npm run dev
```

## Backend
Default API base: http://localhost:8000
You can override by creating `frontend/.env`:
```
VITE_API_BASE=http://localhost:8000
```
